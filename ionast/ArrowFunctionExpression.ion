import
    ion.*
    .*

export
    class ArrowFunctionExpression extends Expression, Scope
        var params: Array<Parameter>
        var body: BlockStatement | Expression
        var expression: Boolean
