import
    .*
    ion.*

export
    class ConditionalDeclaration extends VariableDeclaration
        var negate: Boolean = false
        # the type will be calculated based on the assert and the scoped type in the parent
