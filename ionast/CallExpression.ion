import
    ion.*
    .*

export
    class CallExpression extends Expression, ChainElement
        var new : Boolean = false
        var callee: Expression
        var arguments: Array<Expression | Statement | SpreadElement>
