import
    ion.*
    .*

export
    class Variable extends Meta, Expression, Declaration
        # Expression if it's a [symbol] or something similar on a class variable.
        var value: Expression | Null = null

        var isType: Boolean = false
        var isMeta: Boolean = false
        var isInstance: Boolean = false
        var isStatic: Boolean = false
        var typeParameterIndex: Number | Null = null
