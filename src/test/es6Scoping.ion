# const
#     index = import '../compiler'

#     # This currently works.
#     source0 = ""
#         const a = template ->
#             return 1
#         activate template ->
#             let foo = a()

#     # This doesn't.
#     source1 = ""
#         activate template ->
#             let foo = 1
#         const b = template ->
#             return 1
#         activate template ->
#             let foo = b()

# export const
#     test = () ->
#         let options = {target:'es6'}

#         let js = index.compile(source0, options)
#         console.log(js)
#         eval(js)

#         js = index.compile(source1, options)
#         console.log(js)
#         eval(js)