# let classDeclators = import './classDeclarators'
# export const
#     test = ->
#         assert Object.keys(classDeclators).length is 3

#         assert 81 is Math.max.apply(..)
#             null
#             [i * i for let i = 0; i < 10; i++]

#         assert 1 is Math.min(..)
#             9, 8, 7
#             6, 5, 4
#             3, 2, 1

#         assert JSON.stringify(a:10, b:20) is JSON.stringify(..)
#             a: 10, b: 20

#         assert JSON.stringify(a: 10, b: 20) is JSON.stringify({a: 10, b: 20})

#         let foo = ""
#             {{1}}
#         assert foo[0] is '1'