_js = ""
    (a, b) => ({
        type: "CallExpression",
        callee: {
            type: "MemberExpression",
            object: { type: "Identifier", name: "Math" },
            property: { type: "Identifier", name: "max" }
        },
        arguments: [ a, b ]
    })
@Native(javascript=_js)
max(a: Integer, b: Integer) => 0

@Native(javascript=_js)
max(a: Number, b: Number) => 0
