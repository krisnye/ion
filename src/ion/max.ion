
@Native("(a, b) => ({ ...a, value: Math.max(a.value, b.value) })")
max(a: Integer, b: Integer) => 0

@Native("(a, b) => ({ ...a, value: Math.max(a.value, b.value) })")
max(a: Number, b: Number) => 0
