
// getValue(a: Number[], i: >= 0 & < a.length) => a[i]

// foo = (index: Integer, array: Number[]) =>
//     if index >= 0 && index < array.length
//         getValue(array, index)
//     else
//         -1

foo(index: Integer) =>
    if index >= 0 && index < 10
        index
    else
        index -= 10
        index

