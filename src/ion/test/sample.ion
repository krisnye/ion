// add = (x: Number, y: 0 .. 10) =>
//     if x < 0
//         x
//     else if y < 0
//         y
//     else
//         x + y
// double = (x: Number) => x * 2
// value = 12
// value2 = value.add(10).double.double

@UnitTest([1, 2], 6)
@UnitTest([2, 4], 9)
sample = (a: >= 0, b: Integer) =>
    total = a + b
    for value in [ 0, 1, 2 ]
        total += value
    total
