
@UnitTest(["alpha", /[hlp]+/], [1, "lph"])
@UnitTest(["alpha", /b/], null)
@Native()
    javascript = ""
        (a, pattern, index) => {
            pattern.lastIndex = index;
            let result = pattern.exec(a);
            return result ? [result.index, result[0]] : null;
        }
find(a: String, pattern: RegExp, index: >= 0 = 0): [>= 0, String] | Null => [0, ""]
