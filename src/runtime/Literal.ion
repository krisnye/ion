const ion = import '../'

export class Literal extends import './Expression'
    properties:
        watchValue: (watcher) ->
            watcher(@value)
        unwatchValue: (watcher) ->
            watcher(undefined)