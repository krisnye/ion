// class A
//     a: Number
// class B
//     b: Number
// C = A & B

// both = (): C -> 1

// sample = both()

sample = (a: Array, i: 0 .. < a.length) -> 1

// array = [1,2]
// index: 0..1 = 1
// sample = array[index]

// class Pair<T>
//     a: T
//     b: T
// Percent = 0 .. 100
// sample = Pair<Percent>(1, 1)

// sample: Boolean = false

// class @Foo
//     value: 0..10

// @Foo(value = 10)
// @Foo(10)
// get = (array: Array, index: 0 .. < array.length, optional: Number = 10) ->
//     1

// sample = (a: Array, i: Number) ->
//     // get(a, i)
//     if i >= 0 & i < a.length
//         get(a, i)

// class Vector2
//     x: Number
//     y: Number
// sample = Vector2(0, 0)
// min(1, 2)
