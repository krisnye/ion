
// class @Foo
//     value: 0..10

// @Foo(value = 10)
// @Foo(value = 2)
get = (array: Array, index: 0 .. < array.length, optional: Number = 10) ->
    1

sample = (a: Array, i: Number) ->
    // get(a, i)
    if i >= 0 & i < a.length
        get(a, i)

// class Vector2
//     x: Number
//     y: Number
// sample = Vector2(0, 0)
// min(1, 2)
