
console.log('Compile')

// import * as np from "path"
// import Compiler from "../Compiler"

// let input = np.join(process.cwd(), "src/ion")
// let output = np.join(process.cwd(), "lib/ion")

// let compiler = new Compiler({input, output, namespace:"ion"})
// compiler.compile()
