import
    ion.*

export class Point2
    var x: Number = 0
    var y: Number = 0
    let length2 = (x * x + y * y) ^ 0.5
    var myfunc = ->
        if true
            return false
        else if false
            return true
        else
            return 12

        var array = [1, 2, 3]

        var x = 10
        while x >= 0
            x = x - 1
        for i in x
            x = i
        return 12
