import
    .*

export class Array<T>
    # let Index = Integer where . >= 0 and . < length
    # var length: Integer where . >= 0
    # let indexOf: (element: T, from: Index = 0) -> Index | -1
    # let lastIndexOf: (element: T, from: Index = length - 1) -> Index | -1
