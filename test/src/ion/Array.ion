import
    .Integer

export class Array<T>
    type Index = Integer . >= 0 and . < length
    var length: Integer . >= 0
    let indexOf: (element: T, fromIndex: Index = 0) -> Index | null
    let lastIndexOf: (element: T, fromIndex: Index = 0) -> Index | null
