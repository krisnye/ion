(function(){require.register('ion/reactive/Statement',function(module,exports,require){// Generated by CoffeeScript 1.6.3
(function() {
  var Construct, Expression, Operation, Statement,
    __hasProp = {}.hasOwnProperty,
    __extends = function ___extends(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  Operation = require('./Operation');

  Expression = require('./Expression');

  Construct = require('./Construct');

  module.exports = Statement = (function(_super) {
    __extends(Statement, _super);

    function Statement(properties) {
      Statement.__super__.constructor.call(this, properties);
      if (this.context == null) {
        throw new Error("" + this.constructor.name + ".context is required");
      }
      this;
    }

    Statement.prototype.isActive = false;

    Statement.prototype.activate = function _activate() {
      this.isActive = true;
    };

    Statement.prototype.deactivate = function _deactivate() {
      this.isActive = false;
    };

    Statement.prototype.dispose = function _dispose() {
      Statement.__super__.dispose.call(this);
      if (this.isActive) {
        return deactivate();
      }
    };

    return Statement;

  })(Construct);

}).call(this);

})})()